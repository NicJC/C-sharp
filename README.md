# C-sharp
C# simple / important basics:

* Variables 

```
Console.WriteLine(" C# Variables (various approaches):");
var test = "Hello";
string tst;
tst = "test";
int number = 15;
string name;
name = "Nic";
double dnumber = 0.09d;
decimal decs = 1.15m;
Console.WriteLine(test + "," + "my name is " + name + "," + "this is a " + tst + " plus an integer " + number + "," + " and " + "a double " + dnumber + " and a decimal number " + decs);
```
